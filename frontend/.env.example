# Frontend Environment Variables (Optional)
# If left empty, API requests use relative path '/api' which Vite proxies to backend on port 5000 in dev
VITE_API_URL=
VITE_BACKEND_URL=http://localhost:5000
